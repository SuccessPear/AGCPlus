dataset: fashion-mnist
model: mlp
task: classification
optimizer: sgd
criterion: ce
grad:      # đổi sang none, ags, clipnorm dễ dàng
trainer: default
scheduler:
batch_size: 1
mlflow:
  tracking_uri: "file:mlruns"
  experiment: "cnn_fmnnist_experiments"
  run_name: "MLP_sgd_0.1_agc_noschedule_v3"
  register_as: null            # "ags-cifar10-cnn" để push vào registry
  tags:
    project: "AGC"
    dataset: "MNIST"
    model: "CNN"
    optimizer: "ADAM"
    notes: "Experiment on Fisher AGC, bs small, MLP"
  autolog: false               # nếu muốn bật mlflow.autolog()

ckpt:
  dir: runs/exp1
  best: runs/exp1/best.pt
  last: runs/exp1/last.pt
  save_best_on: "val_loss"