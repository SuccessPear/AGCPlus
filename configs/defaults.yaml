dataset: yfinance_ts
model: rnn
task: regression
optimizer: sgd
criterion: mse
grad:       # đổi sang none, ags, clipnorm dễ dàng
trainer: default
scheduler:
mlflow:
  tracking_uri: "file:mlruns"
  experiment: "lightcnn_Fisher_0.1_0.01WD"
  run_name: "cnn_sgd_0.1_var_noschedule_v3"
  register_as: null            # "ags-cifar10-cnn" để push vào registry
  tags:
    project: "AGC"
    dataset: "CIFAR10"
    model: "CNN"
    optimizer: "AdamW"
    notes: "Experiment on Fisher AGC, bs=512, LightCNN"
  autolog: false               # nếu muốn bật mlflow.autolog()

ckpt:
  dir: runs/exp1
  best: runs/exp1/best.pt
  last: runs/exp1/last.pt
  save_best_on: "val_loss"